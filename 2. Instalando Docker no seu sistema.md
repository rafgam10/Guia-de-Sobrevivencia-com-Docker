
# Instalando Docker no seu sistema

Este guia mostra **como instalar Docker corretamente** nos principais sistemas operacionais e explica **o que está acontecendo em cada etapa**, para você não instalar “no escuro”.

---

## 🧠 Antes de começar: o que você vai instalar?

Ao falar “instalar Docker”, normalmente estamos falando de:

- **Docker Engine** → o motor que roda os containers
- **Docker CLI** → comandos (`docker`, `docker compose`)
- **Docker Desktop** (Windows / macOS) → interface + engine

📌 Em Linux, geralmente **não se usa Docker Desktop**.

---

## 🖥️ Requisitos básicos

- CPU 64 bits
- Virtualização habilitada na BIOS (Windows/macOS)
- Acesso de administrador
- Conexão com a internet

---

## 🐧 Instalando Docker no Linux (Ubuntu / Debian)

### 1️⃣ Atualizar o sistema
```bash
sudo apt update
sudo apt upgrade -y
````

---

### 2️⃣ Instalar dependências

```bash
sudo apt install -y ca-certificates curl gnupg
```

---

### 3️⃣ Adicionar a chave oficial do Docker

```bash
sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
```

---

### 4️⃣ Adicionar o repositório do Docker

```bash
echo \
"deb [arch=$(dpkg --print-architecture) \
signed-by=/etc/apt/keyrings/docker.gpg] \
https://download.docker.com/linux/ubuntu \
$(lsb_release -cs) stable" | \
sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

---

### 5️⃣ Instalar Docker

```bash
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io
```

---

### 6️⃣ Testar instalação

```bash
docker --version
```

---

### 7️⃣ (Opcional, mas recomendado) Usar Docker sem sudo

```bash
sudo usermod -aG docker $USER
```

➡️ Faça logout/login depois disso.

---

## 🪟 Instalando Docker no Windows

### 1️⃣ Requisitos importantes

- Windows 10/11 **64 bits**
- WSL 2 habilitado

---

### 2️⃣ Instalar WSL 2

Abra o PowerShell como administrador:

```powershell
wsl --install
```

Reinicie o computador.

---

### 3️⃣ Baixar Docker Desktop

- Acesse: [https://www.docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop)
- Baixe **Docker Desktop for Windows**
- Execute o instalador

---

### 4️⃣ Durante a instalação

- Marque: **Use WSL 2**
- Aceite os termos

---

### 5️⃣ Verificar instalação

No terminal (PowerShell ou WSL):

```bash
docker --version
docker compose version
```

---

## 🍎 Instalando Docker no macOS

### 1️⃣ Verifique o processador

- Intel → Docker Desktop (Intel)
- Apple Silicon (M1/M2/M3) → Docker Desktop (Apple Chip)

---

### 2️⃣ Baixar Docker Desktop

- [https://www.docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop)
- Baixe a versão correta para seu chip

---

### 3️⃣ Instalar

- Arraste o Docker para a pasta Applications
- Abra o Docker
- Conceda permissões solicitadas

---

### 4️⃣ Verificar instalação

```bash
docker --version
docker compose version
```

---

## ✅ Testando se tudo funciona

Rode o container de teste oficial:

```bash
docker run hello-world
```

Se aparecer uma mensagem de sucesso:

> 🎉 Docker está funcionando corretamente!

---

## 🧰 Docker Compose (já vem na maioria dos casos)

Hoje o Docker Compose já vem integrado:

```bash
docker compose version
```

📌 Se funcionar, você não precisa instalar nada extra.

---

## ⚠️ Problemas comuns e soluções

### ❌ “Docker daemon is not running”

- Abra o Docker Desktop
- Aguarde ele inicializar completamente

---

### ❌ Permissão negada (Linux)

```bash
sudo usermod -aG docker $USER
```

Depois, logout/login.

---

### ❌ Virtualização desativada

- Ative na BIOS
- No Windows, verifique Hyper-V / WSL 2

---

## 🧑‍💻 Boas práticas após instalar

- Não rode tudo como root
- Use `.env` para variáveis
- Prefira Docker Compose
- Limpe containers/imagens antigos

Comandos úteis:

```bash
docker ps
docker images
docker system prune
```
