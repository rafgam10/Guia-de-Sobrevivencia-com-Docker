# Comandos básicos com Docker

Este guia reúne os **principais comandos do Docker** que você vai usar no dia a dia como dev backend ou full stack.

Não é para decorar — é para **entender quando usar cada um**.

---

## 🐳 Estrutura básica de um comando Docker

```bash
docker <objeto> <ação> [opções]
````

Exemplos:

- `docker image ls`
- `docker container rm`
- `docker run`

---

## 📦 Trabalhando com imagens

### 🔹 Listar imagens

```bash
docker images
```

ou

```bash
docker image ls
```

---

### 🔹 Baixar uma imagem

```bash
docker pull python:3.11
```

📌 Baixa a imagem do Docker Hub.

---

### 🔹 Remover imagem

```bash
docker rmi nome_da_imagem
```

---

## ▶️ Trabalhando com containers

### 🔹 Criar e rodar um container

```bash
docker run nome_da_imagem
```

Exemplo:

```bash
docker run python:3.11
```

---

### 🔹 Rodar em modo interativo

```bash
docker run -it ubuntu bash
```

- `-i` → interativo    
- `-t` → terminal

---

### 🔹 Rodar em background (detached)

```bash
docker run -d nginx
```

---

### 🔹 Listar containers rodando

```bash
docker ps
```

---

### 🔹 Listar todos os containers

```bash
docker ps -a
```

---

### 🔹 Parar um container

```bash
docker stop <container_id>
```

---

### 🔹 Iniciar container parado

```bash
docker start <container_id>
```

---

### 🔹 Remover container

```bash
docker rm <container_id>
```

---

### 🔹 Acessar container em execução

```bash
docker exec -it <container_id> bash
```

---

## 🌐 Portas e volumes

### 🔹 Mapear portas

```bash
docker run -p 5000:5000 flask_app
```

Formato:

```
porta_host:porta_container
```

---

### 🔹 Usar volumes

```bash
docker run -v $(pwd):/app flask_app
```

📌 Sincroniza arquivos do host com o container.

---

## 🧱 Dockerfile

### 🔹 Buildar uma imagem

```bash
docker build -t nome_da_imagem .
```

---

### 🔹 Exemplo prático

```bash
docker build -t flask_api .
docker run -p 5000:5000 flask_api
```

---

## 📋 Logs e inspeção

### 🔹 Ver logs do container

```bash
docker logs <container_id>
```

---

### 🔹 Acompanhar logs em tempo real

```bash
docker logs -f <container_id>
```

---

### 🔹 Inspecionar container

```bash
docker inspect <container_id>
```

---

## 🧹 Limpeza (muito importante)

### 🔹 Remover containers parados

```bash
docker container prune
```

---

### 🔹 Remover imagens não usadas

```bash
docker image prune
```

---

### 🔹 Limpar tudo que não está em uso

```bash
docker system prune
```

⚠️ Cuidado: remove containers, imagens e redes não usadas.

---

## 🧩 Docker Compose (básico)

### 🔹 Subir serviços

```bash
docker compose up
```

---

### 🔹 Subir em background

```bash
docker compose up -d
```

---

### 🔹 Parar serviços

```bash
docker compose down
```

---

### 🔹 Rebuildar imagens

```bash
docker compose up --build
```

---

## 🧠 Dicas importantes

- Prefira `docker compose` a vários `docker run`
- Dê nomes aos containers:

```bash
docker run --name meu_container nginx
```

- Use `.env` para configs
- Não rode containers desnecessários

---

## 📌 Resumo rápido

- `docker run` → cria e roda container
- `docker ps` → lista containers
- `docker stop/start/rm` → controla containers
- `docker build` → cria imagem
- `docker logs` → debug
- `docker compose` → múltiplos serviços
