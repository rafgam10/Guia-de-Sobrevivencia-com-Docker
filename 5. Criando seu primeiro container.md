
# Criando seu primeiro container Docker

Neste guia você vai **criar e rodar seu primeiro container**, entendendo **o que acontece em cada passo**.  
Nada de mágica — só o essencial para começar bem.

---

## 🎯 Objetivo

Ao final, você vai:
- rodar um container simples
- entender a diferença entre imagem e container
- criar um container a partir de uma aplicação
- saber parar, listar e remover containers

---

## 🧠 Pré-requisitos

- Docker instalado
- Terminal aberto
- Docker funcionando (`docker --version`)

Se ainda não instalou, veja o guia **Instalando Docker no seu sistema**.

---

## 🐳 Passo 1 — Rodando seu primeiro container

Vamos usar a imagem oficial de teste do Docker:

```bash
docker run hello-world
````

### O que acontece aqui?

1. Docker procura a imagem `hello-world` localmente
2. Não encontra → baixa do Docker Hub
3. Cria um container a partir da imagem
4. Executa o container
5. Exibe a mensagem de sucesso
6. Encerra o container

🎉 Parabéns! Você acabou de rodar seu primeiro container.

---

## 📦 Passo 2 — Entendendo o que foi criado

### Listar containers (inclusive parados)

```bash
docker ps -a
```

Você verá algo como:

- um container criado
- status: `Exited`

📌 Containers podem existir mesmo depois de parar.

---

## ▶️ Passo 3 — Criando um container interativo

Agora vamos rodar algo mais “real”.

```bash
docker run -it ubuntu bash
```

### O que isso faz?

- `ubuntu` → imagem base
- `-it` → modo interativo
- `bash` → comando executado no container

Você agora está **dentro do container Ubuntu**.

Teste:

```bash
ls
cat /etc/os-release
```

Para sair:

```bash
exit
```

---

## 🧭 Passo 4 — Containers em background

Vamos rodar um servidor web simples:

```bash
docker run -d nginx
```

- `-d` → detached (background)
- `nginx` → servidor web

### Ver containers rodando

```bash
docker ps
```

---

## 🌐 Passo 5 — Expondo portas

Para acessar o Nginx no navegador:

```bash
docker run -d -p 8080:80 nginx
```

- `8080` → porta do seu computador
- `80` → porta do container

Abra no navegador:

```
http://localhost:8080
```

---

## 🛑 Passo 6 — Parar e remover containers

### Parar container

```bash
docker stop <container_id>
```

### Remover container

```bash
docker rm <container_id>
```

💡 Dica:

```bash
docker ps -aq
```

Lista apenas os IDs.

---

## 🧱 Passo 7 — Criando seu primeiro container com Dockerfile

Agora algo mais profissional.

### 1️⃣ Crie um arquivo `Dockerfile`

```dockerfile
FROM python:3.11
WORKDIR /app
CMD ["python", "-c", "print('Meu primeiro container Docker 🚀')"]
```

---

### 2️⃣ Build da imagem

```bash
docker build -t meu-primeiro-container .
```

---

### 3️⃣ Rodar o container

```bash
docker run meu-primeiro-container
```

🎉 Você criou e executou **seu próprio container**.

---

## 🧠 O que você aprendeu até aqui

- `docker run` cria **containers**
- Containers vêm de **imagens**
- Containers podem ser interativos ou em background
- Portas permitem acesso externo
- Dockerfile define como a imagem é criada

---

## ⚠️ Erros comuns de iniciantes

- ❌ Achar que container é imagem
- ❌ Alterar coisas no container esperando salvar
- ❌ Não remover containers antigos
- ❌ Rodar tudo como root sem necessidade

---

## 📌 Resumo rápido

- Container = aplicação em execução
- `docker run` → cria e executa
- `-it` → interativo
- `-d` → background
- `-p` → portas
- Dockerfile → cria imagens personalizadas
